const defaultState = [
  {
    nombre: 'Leo'
  },
  {
    nombre: 'Amparo'
  },
]

function usuarios (state = defaultState, action) {
  switch (action.type) {

    default:
      return state;
  }
}

export default usuarios;