const defaultState = [
  {
    total: '100'
  },
  {
    total: '150'
  },
]

function ordenes (state = defaultState, action) {
  switch (action.type) {

    default:
      return state;
  }
}

export default ordenes;